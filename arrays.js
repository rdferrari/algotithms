// Ordered lists

// Searching O(N)
// Access O(1)

// Add or removing array in the beginning O(N)
// Add or removing array in the end O(1)

// Sort O(N * log N)

// map, reduce, filter O(N)

let names = ["Ro", "Lu", "Vi"];

names.push("Mi");

console.log(names);

newNames = names.slice(1, 3);

// ["Lu", "Vi"];

console.log(newNames);
